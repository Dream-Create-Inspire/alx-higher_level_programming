#!/usr/bin/node
// Create an array with the strings to be printed
const messages = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

// Loop through the array and print each message
for (const message of messages) {
  console.log(message);
}

